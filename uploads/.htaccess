# All patient-related uploads are private and must be streamed through the
# authenticated API endpoint, which checks record ownership.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>

<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
